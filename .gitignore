build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Generated by tools/fetch_bmi270_config.sh. Unlike components/fatfs/ in the
# USB-A example, this one is a byte-exact fetch from a pinned upstream and
# carries Bosch's BSD-3-Clause header rather than this repo's MIT, so it is
# regenerated rather than committed.
main/bmi270_config_file.h

# Vendored by tools/fetch_bosch_drivers.sh — byte-exact fetches from pinned
# upstreams, BSD-3-Clause rather than this repo's MIT.
components/bosch_sensortec/
